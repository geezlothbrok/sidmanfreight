# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Google Fonts' raw Inter download. Unused: the app loads Inter via
# @fontsource-variable/inter, which ships per-language subsets, so a visitor
# fetches ~47 KB (latin only) instead of the 340 KB unsubsetted file.
# Kept out of git rather than committed — see the note in src/index.css.
src/assets/fonts/Inter/
