# dependencies
node_modules/
client/node_modules/

# build output
client/dist/
client/build/

# local secrets — never commit
backend/auth_config.php
backend/.env
backend/.env.production
.env
.env.local

# Google Fonts' raw Inter download. Unused: the app loads Inter via
# @fontsource-variable/inter, which ships per-language subsets.
client/src/assets/fonts/Inter/

# os / editor
.DS_Store
*.log
