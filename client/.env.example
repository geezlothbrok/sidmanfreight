# Absolute URL of the staff-portal API (backend/manager_api.php).
# Leave UNSET for local dev: the app falls back to /backend/manager_api.php,
# which vite.config.ts proxies to http://127.0.0.1:8000.
#   Run the backend with:  php -S 127.0.0.1:8000 -t ../backend
VITE_API_URL=https://api.sidmanfreightconsult.com/manager_api.php
