/**
 * One page gutter for the whole site. The navbar, hero, page sections, and
 * footer all share it so their left and right edges line up at every width.
 */
export const pageContainer =
  "mx-auto w-full max-w-[1920px] px-6 sm:px-10 lg:px-16 xl:px-24"
