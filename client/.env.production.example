# Frontend production environment (set in the Vercel project settings).
#
# Without this the app falls back to https://api.sidmanfreightconsult.com/manager_api.php,
# so set it only if the API lives somewhere else.
VITE_API_URL=https://api.sidmanfreightconsult.com/manager_api.php
